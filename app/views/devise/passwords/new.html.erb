<h1 class="tituloSesion">¿Olvidó su contraseña?</h1>
<h2 class="tituloSesion"> Ingrese su mail para recibir instrucciones de recuperación de contraseña<h2>
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { class: 'form-signin', method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.email_field :email, :class => 'form-control', :placeholder => 'Correo electrónico' %>
  </div>
  
  <div class="actions">
    <%= f.submit "Recuperar contraseña", :class => 'btn btn-lg btn-block' %>
  </div>
	 <!--%= render "devise/shared/links" %-->
<% end %>

