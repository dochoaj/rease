<%= link_to users_path, :class=>'btn btnAside' do %>
	¡Hola <%=current_user.nickname%>!
	<%if current_user.photo.present?%>
		<%= image_tag(current_user.photo, :class=>'SquarePicture') %>
	<%else%>
		<i class="glyphicon glyphicon-user"></i>
	<%end%>	
<% end %>