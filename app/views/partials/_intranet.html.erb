<% if !user_signed_in? %>
	<div id="panelAside" class="panel panel-default" data-toggle="collapse" data-target="#cuerpo">
		<div class="panel-heading">
			Ingreso Intranet
		</div>
		<div class="collapse" id="cuerpo">
			<div class="panel-body" id="styleSession" >
		    	<%= render "devise/sessions/inicio_sesion" %>
			</div>	
		</div>
	</div>
<%end%>
