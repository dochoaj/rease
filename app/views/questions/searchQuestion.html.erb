<h1>Búsqueda de preguntas frecuentes</h1>

<div class="buscador">
	<%= form_tag(questions_searchQuestion_path, :method => "post",:class=>"form-inline") do %>
	<div class="input-group">
		<%= text_field_tag :search, params[:search], placeholder: "Buscar pregunta",:class => 'form-control',:required=>true %>
		<span class="input-group-btn">
			<%= button_tag(type: "submit", class: "btn btn-default ") do %>
				<i class="glyphicon glyphicon-search"></i>
			<% end %>
		</span>
	</div>
	<%end%>
</div>

<% if @questions.present? %>
	<div class="panel panel-default panelRease">
		<div class="panel-heading">
			Resultados de la búsqueda: <%= params[:search] %>
		</div>
		<div class="panel-body">
			<table class="tabla-rease">
				<tr>
					<th><strong>Tipo</strong></th>
					<th><strong>Nombre</strong></th>
					<th><strong>Detalles</strong></th>

				</tr>
					<%= render @questions %>
			</table>
		</div>
	</div>
<%else%>
<div class="panel panel-default panelRease">
	<div class="panel-heading">
		Eventos: <%= params[:search] %>
	</div>
	<div class="panel-body">
		<p>No se encontraron resultados en la búsqueda de <strong><%= params[:search] %></strong></p>
	</div>
</div>
<% end %>

<%=link_to "Volver", presentation_about_path, :class=>'btn btn-lg btnCRUD'%>
